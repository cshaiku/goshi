action: fs.read
description: Read a single file within the allowed repository scope

request:
  path:
    type: string
    constraints:
      - relative
      - no_symlinks
      - repo_scoped
    required: true

  reason:
    type: string
    required: true

response:
  content:
    type: string
