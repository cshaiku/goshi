<!--
  FILE STATUS DOCBLOCK
  ====================
  Status: COMPLETED
  Completion Date: 2026-02-10
  Completed By: Automated CLI UX Improvement Sprint
  Duration: Single session (2026-02-10)
  
  Related Commits:
  - 85c9fe6: docs: Add EXIT CODES documentation to all CLI commands
  
  Completion Notes:
  - Added EXIT CODES sections documenting success (0) and error (1/2/3) codes to all commands
  - Covered commands: config show, config validate, config init, fs read, fs list, fs write, fs apply
  - Standardized format across all exit code documentation
  - Tested all commands with --help to verify EXIT CODES are visible
  - Changes committed and merged to main
  
  Summary:
  Successfully added comprehensive EXIT CODES documentation to all goshi CLI commands,
  enabling scripting users to properly handle command results based on exit codes.
  All tasks completed and tested.
-->

# Goshi CLI Exit Code Documentation Action Plan

**Created:** 2026-02-10-00-00  
**Status:** ✅ COMPLETED

## Goal
Ensure all goshi CLI commands that may fail document their exit codes in help output for scripting users.

## Tasks
- ✅ Review all commands for exit code behavior.
- ✅ Add or update exit code documentation in help text for each relevant command.
- ✅ Test help output for exit code clarity.
- ✅ Commit changes with a summary referencing this action plan.

## Implementation Summary
- Added EXIT CODES sections to `config show`, `config validate`, `config init` commands
- Added EXIT CODES sections to `fs read`, `fs list`, `fs write`, `fs apply` commands
- Documented success codes (0) and error codes (1, 2, 3) appropriate to each command
- Standardized EXIT CODES format and placement relative to other help sections
- Tested all commands with `--help` to verify EXIT CODES documentation is visible
- Changes committed and merged to main branch

---
